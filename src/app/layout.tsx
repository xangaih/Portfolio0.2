import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Khangai Enkhbat — Software Engineer",
  description:
    "Portfolio of Khangai Enkhbat — CS & Data Science graduate from DePauw University. Builder of AI tools, full-stack applications, and scalable systems.",
  keywords: ["software engineer", "AI", "full-stack", "portfolio", "Khangai Enkhbat"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
